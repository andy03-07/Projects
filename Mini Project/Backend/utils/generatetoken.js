// const jwt = require("jsonwebtoken");

// const generateToken = (id) => {
//   return jwt.sign({ id }, process.env.JWT_SECRET, { expiresIn: "30d" });
// };

// console.log("Token generated:", generateToken('newUser._id'));

// module.exports = generateToken;
